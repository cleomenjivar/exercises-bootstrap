CREATE TABLE `johnson` (
  `id`  INTEGER PRIMARY KEY AUTOINCREMENT,
  `name` TEXT,
  `age` INTEGER,
  `specialty` TEXT
);

INSERT INTO johnson (name, age, specialty) VALUES ('Snoop Diog', 1000, "Ruby");

INSERT INTO johnson (name, age, specialty) VALUES ('P to the Y', 1000. "hacking your instagrams");

.mode column

.headers on

SELECT * FROM johnson;
